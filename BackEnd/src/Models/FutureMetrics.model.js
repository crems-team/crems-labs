class FutureMetrics {
    constructor(newListings, existListing,pendingListings) {
        this.newListings 	   =   newListings ;
        this.existListing      =   existListing ;
        this.pendingListings   =   pendingListings ;
                
    }
}
module.exports = FutureMetrics;